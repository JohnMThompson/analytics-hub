# Local Agent Instructions

- When the user asks to start localhost, start both services by default:
  frontend with `npm run dev` from `/home/john/git-repos/ai-analytics/frontend`
  backend with `python3 backend/app.py` from `/home/john/git-repos/ai-analytics`
